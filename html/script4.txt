#!/bin/bash
## find smallest number
if [ $1 -lt $2 ]
then
	if [ $1 -lt $3 ]
	then
		echo " $1 is smallest number"
	else
		echo " $3 is smallest number"
	fi
else
	if [ $2 -lt $3 ]
	then
		echo " $2 is smallest number"
	else
		echo " $3 is smallest number"
	fi
fi

